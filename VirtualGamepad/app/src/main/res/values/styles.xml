<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="match_parent_width_wrap_content_height">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Edit text in ConnectionActivity -->
    <style name="conn_activity_edit_text" parent="match_parent_width_wrap_content_height">
        <item name="android:focusable">true</item>
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:textColor">@color/grey</item>
        <item name="android:textSize">@dimen/font_normal_14</item>
    </style>

    <!-- Dpad buttons in MainActivity -->
    <style name="dpad_btn">
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:textSize">@dimen/font_medium_20</item>
    </style>

    <!-- Action buttons in MainActivity -->
    <style name="act_btn">
        <item name="android:layout_width">55dp</item>
        <item name="android:layout_height">55dp</item>
        <item name="android:textSize">@dimen/font_medium_20</item>
    </style>

</resources>